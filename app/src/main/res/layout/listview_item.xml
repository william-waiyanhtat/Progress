<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/spacing_normal"
    android:orientation="vertical">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:id="@+id/tv_title_widget_list"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:text="ABC"
            android:layout_weight="1"
            android:textSize="@dimen/text_normal"
            tools:text="Anniversary" />

        <TextView
            android:id="@+id/tv_percent_widget_list"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="right"
            android:gravity="bottom"
            tools:text="0%" />
    </LinearLayout>


    <ProgressBar
        android:id="@+id/progress_widget_list"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:progress="0"
        android:visibility="gone"/>

    <ImageView
        android:id="@+id/progress_widget_list_imgv"
        android:layout_width="match_parent"
        android:scaleType="fitXY"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="2dp"
        android:layout_height="8dp"/>

    <TextView
        android:id="@+id/tv_detail_widget_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:textSize="@dimen/text_nsmall"
        tools:text="3Month(s) 4Day(s) Left" />

</LinearLayout>